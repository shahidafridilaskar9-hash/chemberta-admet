# ChemBERTa-ADMET — Dependencies
# Python 3.9 or higher recommended (tested on Python 3.13)
# Install core dependencies:
#   pip install -r requirements.txt
# For ChemBERTa fine-tuning (notebooks 03-04), also install:
#   pip install torch transformers

# ── Chemistry ────────────────────────────────────────────────
rdkit>=2023.3.1
# Note: deepchem is NOT required — we use direct dataset download
# via src/download_data.py which is Python 3.13 compatible

# ── Machine Learning ──────────────────────────────────────────
scikit-learn>=1.3.0
xgboost>=2.0.0
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0

# ── Transformers (for ChemBERTa fine-tuning) ──────────────────
torch>=2.0.0
transformers>=4.35.0
tokenizers>=0.15.0

# ── Uncertainty / Conformal Prediction ───────────────────────
mapie>=0.8.0

# ── Visualisation ─────────────────────────────────────────────
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.15.0

# ── Notebooks ────────────────────────────────────────────────
jupyter>=1.0.0
ipykernel>=6.20.0
ipywidgets>=8.0.0

# ── Utilities ─────────────────────────────────────────────────
tqdm>=4.64.0
requests>=2.28.0
