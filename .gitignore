# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/
*.egg
.env
.venv/
env/

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints

# Data (downloaded at runtime via src/download_data.py — not committed)
data/*.csv
data/*.sdf
data/*.pkl
data/*.gz
!data/.gitkeep

# Results — keep JSON benchmark files, exclude large model artefacts
results/*.pt
results/*.pth
results/*.pkl
# baseline_results.json is intentionally tracked (real benchmark numbers)

# Figures (regenerated by notebooks)
figures/*.png
figures/*.pdf
!figures/.gitkeep

# OS
.DS_Store
Thumbs.db
Thumbs.db:encryptable
desktop.ini
